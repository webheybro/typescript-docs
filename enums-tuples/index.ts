//Enums
//List 
enum WeekDays {
    Monday = 0, 
    Tuesday,
    Wednesday,
}
console.log(WeekDays[0]); // lundi

//Tuples : Ferme à un nombre de donné et à un type
let identity: [string, number] = ["Alex", 27];
console.log(identity);