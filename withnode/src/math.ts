
    export const addition = (x:number, y:number) => x + y;
    export const multiplication = (x:number, y:number) => x * y;
    export const soustraction = (x:number, y:number) => x - y;
    export const division = (x:number, y:number) => x / y;   
